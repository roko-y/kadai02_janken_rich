// jsを記述する際はここに記載していく

    // alert(11111);

// $(document).ready(function() {
//     new Splide('#image-carousel').mount();
// });

$('.slider').slick({
    autoplay: true, // 自動再生オン
    speed: 1000, // 画像切り替え速度
    fade: true, // フェードで画像が切り替わる
});